#Student Portal

This is the Student Portal website

Local Branch : main
Remote Branch : origin/main

git push -u origin main

origin = the remote repository (GitHub repository).
main = the local branch that you are pushing.Local Branch : main
Remote Branch : origin/main

git push -u origin main

origin = the remote repository (GitHub repository).
main = the local branch that you are pushing.


